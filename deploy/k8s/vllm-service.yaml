apiVersion: v1
kind: Service
metadata:
  name: vllm-qwen
  namespace: llm-serving
spec:
  selector:
    app: vllm-qwen
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP

